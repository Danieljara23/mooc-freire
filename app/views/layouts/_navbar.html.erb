<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "CICLO DE FORMACIÓN FREIRE", root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if user_signed_in? %>
       <ul class="nav navbar-nav">
         <li><%= link_to 'Cursos', courses_path %></li>
       </ul>
     <% end %>

      <%= search_form_for(@q, url: root_path, html: {class: 'navbar-form navbar-left'}) do |f| %>
        <div class="form-group">
          <%= f.search_field :title_or_description_cont, class: 'form-control', placeholder: 'Buscar pregunta' %>
        </div>
        <%= f.submit "Buscar", class: 'btn btn-default' %>
      <% end %>


      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <span class="glyphicon glyphicon-plus"></span> <%= current_user.email.split("@")[0] %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to raw("<span class='glyphicon glyphicon-remove'></span> Cerrar Sesión"), destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to raw("<span class='glyphicon glyphicon-user'></span> Inicia sesión"), new_user_session_path %> </li>
          <li><%= link_to raw("<span class='glyphicon glyphicon-plus'></span> Registrate"), new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
